# 暴汗艺术家 · 答辩前环境自检清单

> 全部用 conda 环境 `HPU-3.12`（`& "D:\Program Files\miniconda3\envs\HPU-3.12\python.exe"`）。
> 控制台跑脚本加 `PYTHONUTF8=1` 规避 emoji 编码报错。
> **最关键两步**：① 重刷 seed（否则演示脏数据/旧值）② 重启 `api_server.py`（否则跑的是旧代码）。

## 0. 数据库
- [ ] PostgreSQL `hpu_db` 在跑
- [ ] 已按序执行：`backend/sql/tables_v3.1_full.sql` → `backend/sql/migrate_v3_1_aligned.sql`（幂等，可重跑）

## 1. 重刷干净历史数据
- [ ] `PYTHONUTF8=1 python backend/scripts/seed_week_history_v31.py`
      预期：user=1 向好 / user=2 恶化，各 14 天；含 user_plans / user_executions / sleep_log
- [ ] 无遗留"未来日期"测试行：`SELECT date FROM watch_data WHERE date > CURRENT_DATE;` → 应为空

## 2. 起后端（必须重启，使换源/写回代码生效）
- [ ] `& "D:\Program Files\miniconda3\envs\HPU-3.12\python.exe" backend/api_server.py`
- [ ] `GET /health` → healthy

## 3. 后端接口冒烟（浏览器 /docs 或 curl）
- [ ] `/dashboard/1` —— KPI/身体/睡眠/饮食/运动面板有数
- [ ] `/sleep/1` —— total_hours≈8、达标、趋势 7 点；sources 多为 db/derived
- [ ] `/sleep/1?range=14d` —— 14 个点
- [ ] `/exercise/1` —— 今日总览有步数、今日记录 3 条(户外跑步含距离)、本周趋势、成就 percentile=derived
- [ ] `/nutrition/1?date=今天` —— KPI/能量(缺口=目标−已摄入)/分餐含单品/运动建议
- [ ] `POST /sleep/entry {bedtime,wake_time}` —— 正常 200 saved=true；非法(起床早于入睡)→422
- [ ] `/exercise/3`（goals=NULL 用户）—— 不报 500，回退正常

## 4. /plan 多智能体 + 计划写回（走真实 LLM，确认 API key）
- [ ] `POST /plan {user_id:1, mode:"control"}` → 拿 task_id
- [ ] 轮询 `/status/{task_id}` 到 completed，`result.final_report` 有 chart_data
- [ ] `user_plans` 当日行 `source='agent'`（被本次产出覆盖）
- [ ] 再看 `/exercise/1`、`/nutrition/1`：建议/方案变为本次 agent 内容
- [ ] `mode:"experiment"` 再跑一次可对比
- [ ] （可选）处方药/危险动作提问 → `/chat` 返回 `intent=blocked` 熔断话术

## 5. 起前端
- [ ] `npm --prefix frontend run dev`（Vite 代理 `/api` → localhost:8000）
- [ ] 四页逐一过：
  - `/report` —— Header(标题+时间+用户+导出)、KPI、身体指标、睡眠环+柱、饮食环、运动监测、健康建议、聊天
  - `/exercise` —— Banner、今日总览(进度)、今日状态、运动记录、本周趋势(ECharts)、运动建议、成就
  - `/sleep` —— 评分环+插画、睡眠时间/小憩达标、今日睡眠情况、趋势(双轴+7/14切换)、个性化建议(tab+食物图标)、录入表单
  - `/nutrition` —— 日期切换+4卡、能量环形+宏量、分餐记录(食物图标)、运动建议、能康助手聊天
- [ ] 食物/动作图标全部显示（无首字占位）
- [ ] 睡眠页"保存记录" → 成功后今日睡眠情况回填
- [ ] 饮食页日期左右切换 → 数据刷新
- [ ] 窗口拖窄到 <1120 → 右侧内容不压缩、出现横向滚动条（非空白）

## 6. 兜底验证
- [ ] `npm --prefix frontend run build`（vue-tsc + vite build）通过
- [ ] 浏览器控制台无 error；网络无 4xx/5xx（除非故意触发）
